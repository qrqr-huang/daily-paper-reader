---
title: "KATANA: A Fast, Low-Power Mapping of Kalman Filters onto Edge NPUs for Real-Time Tracking"
title_zh: KATANA：一种将卡尔曼滤波器快速低功耗映射到边缘 NPU 以用于实时跟踪的方法
authors: "Bodhisatwa Kundu, Anish Rooj, Sumit Saha, Abhradeep Sarkar, Arghadip Das, Arnab Raha, Mrinal K. Naskar"
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.14992v1"
tags: ["query:lfp-soc"]
score: 8.0
evidence: 将卡尔曼滤波器映射到边缘NPU，实现低功耗实时状态估计，可直接用于SOC估计。
tldr: "边缘实时跟踪系统（如雷达、无人机、自动驾驶）面临严格功耗与延迟限制，传统卡尔曼滤波器在CPU上串行执行或依赖专用FPGA/ASIC加速器。KATANA首次将LKF和EKF端到端映射到商用NPU，通过减转加、张量融合和块对角批处理三大图优化，实现100%矩阵引擎运算。在Intel Core Ultra Series 2上，EKF达223 FPS/13.4W，LKF达409 FPS/14.1W，动态能耗较CPU降低97.9%。该框架为边缘平台提供了不占用CPU/GPU的实时跟踪低功耗方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.14992v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 643, \"height\": 258, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14992v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 907, \"height\": 268, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14992v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 916, \"height\": 1328, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14992v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 909, \"height\": 287, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14992v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 902, \"height\": 240, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.14992v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1878, \"height\": 341, \"label\": \"Table\"}]"
motivation: 边缘实时跟踪系统需在严格功耗预算内低延迟融合测量，现有卡尔曼滤波在CPU上串行执行或需定制加速器，难以兼顾功耗与灵活性。
method: 提出KATANA框架，通过减转加（预计算负投影矩阵）、静态形状张量融合和块对角批处理并行化，将LKF/EKF所有操作映射到NPU矩阵引擎。
result: "在Intel Core Ultra Series 2 NPU上，批处理EKF达223.35 FPS@13.43W，LKF达408.73 FPS@14.05W，相比CPU动态能耗降低97.9%。"
conclusion: KATANA证明卡尔曼滤波器可高效映射到现有NPU，实现实时低功耗跟踪，避免专用硬件，释放CPU/GPU用于其他任务，为边缘AI跟踪提供新范式。
---

## 摘要
状态估计是每个实时跟踪系统的闭环核心，从雷达监视和反无人机防御到自动驾驶和机器人技术。这些部署在边缘平台上运行，其中防御系统安装在车辆和无人机上，民用流程则运行在汽车和手持设备上。在这里，每多消耗一瓦的计算量都会缩短任务持续时间或操作范围。由此产生两个硬约束：每个新测量必须在下一个控制周期之前融合，且总计算量必须适应严格的电池和热功耗包络。线性和扩展卡尔曼滤波器（LKF, EKF）是这些系统上的主要估计器，但如今它们几乎完全在 CPU 上执行，这会串行化多目标跟踪（MOT）更新，或是在延长设计周期的定制 FPGA/ASIC 加速器上执行。当代 AI-PC SoC，如英特尔酷睿 Ultra 系列 1 和 2，集成了低功耗、数据并行的神经处理单元（NPU）。因此，我们探究是否可以将卡尔曼滤波器映射到这个现有的矩阵引擎上，以同时满足实时和低功耗预算，从而避免使用专用加速器，并让 CPU 和 GPU 可自由处理主要工作负载。我们提出了 KATANA，一个面向 NPU 的优化框架，首次将 LKF 和 EKF 端到端地映射到商用 NPU 上，并在已出货的 AI-PC 芯片上进行了跨平台表征。KATANA 应用了三种代数图重写：通过预计算的负投影矩阵 H_neg 进行的减法到加法重构、静态形状张量融合以及块对角批处理并行化，确保 100% 的操作在 DPU 矩阵引擎上执行。在 Series 2 上，优化后的批处理 EKF 以 13.43 W 有效功耗达到 223.35 FPS，LKF 以 14.05 W 达到 408.73 FPS，与 CPU 实现相比，动态能耗最多降低了 97.9%。

## Abstract
State estimation is the closed-loop core of every real-time tracking system, from radar surveillance and counter-UAV defense to autonomous driving and robotics. These deployments run on edge platforms, where defense systems mount on vehicles and drones, and civilian pipelines live on cars and handheld devices. Here, every additional watt of compute erodes mission duration or operational range. Two hard constraints follow: each new measurement must be fused before the next control cycle, and the total compute must fit within a strict battery and thermal power envelope. The Linear and Extended Kalman Filters (LKF, EKF) are dominant estimators on these systems, but today they execute almost exclusively on CPUs, which serialize multi-object tracking (MOT) updates, or on custom FPGA/ASIC accelerators that lengthen design cycles. Contemporary AI-PC SoCs, like the Intel Core Ultra Series 1 and 2, integrate a low-power, data-parallel Neural Processing Unit (NPU). We therefore ask whether the Kalman filter can be mapped onto this existing matrix engine to meet real-time and low-power budgets simultaneously, avoiding a dedicated accelerator and keeping the CPU and GPU free for primary workloads. We present KATANA, an NPU-aware optimization framework delivering the first end-to-end mapping of the LKF and EKF onto a commercial NPU, alongside a cross-platform characterization on shipping AI-PC silicon. KATANA applies three algebraic graph rewrites: subtract-to-add reformulation via a precomputed negative-projection matrix H_neg, static-shape tensor fusion, and block-diagonal batched parallelization, ensuring 100% of operations execute on the DPU matrix engine. On the Series 2, the optimized batched EKF reaches 223.35 FPS at 13.43 W active power, and the LKF reaches 408.73 FPS at 14.05 W, delivering up to a 97.9% reduction in dynamic energy versus the CPU implementation.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

*   **核心挑战**：边缘实时跟踪系统（如雷达、无人机防御、自动驾驶）面临严格的双重约束：
    *   **延迟**：必须在毫秒级内完成多目标状态估计。
    *   **功耗**：需在电池和散热受限的边缘平台上实现极低功耗计算。
*   **现状与瓶颈**：经典的线性/扩展卡尔曼滤波器（LKF/EKF）是主流算法，但其部署存在缺陷：
    *   **CPU执行**：串行化处理多目标跟踪（MOT），导致吞吐量受限且功耗高。
    *   **FPGA/ASIC加速**：虽然能效高，但开发周期长、设计成本高，且在当前已集成的SoC中增加了额外的芯片面积。
*   **研究动机**：现代AI-PC SoC（如Intel Core Ultra系列）已集成低功耗、数据并行的神经处理单元（NPU）。本研究旨在探索能否将卡尔曼滤波器完全映射到该闲置的NPU矩阵引擎上，以同时实现实时性、低功耗，并释放CPU和GPU用于其他核心任务，从而避免定制加速器。
*   **核心贡献**：提出了**KATANA**框架，首次将LKF和EKF端到端地映射到商用NPU，并通过跨平台（CPU, GPU, NPU）测试验证了其显著优势。

### 2. 论文提出的方法论

*   **核心思想**：通过一系列代数图优化重写，将卡尔曼滤波的所有操作强制转换为NPU矩阵引擎（DPU）的原生密集矩阵乘加（GEMM/Add）操作，**消除任何需要回退到标量DSP的操作**（如Subtract, Reshape, Transpose），从而避免昂贵的DPU↔DSP上下文切换开销。
*   **关键技术细节**：
    1.  **减法到加法重构**：
        *   **问题**：标准KF的创新计算 `yk = zk - H·x̂k|k-1` 包含减法，会强制NPU编译器将其分配至DSP执行。
        *   **方案**：在模型初始化时，预计算一个恒定的负投影矩阵 **Hneg = -H**。
        *   **结果**：创新方程变为 `yk = zk + Hneg·x̂k|k-1`，将减法转换为DPU原生的矩阵乘法和元素加法。此法推广至迭代过程中所有减法。
    2.  **静态形状张量融合**：
        *   **问题**：ONNX导出器引入的动态批处理轴和运行时Reshape、Transpose操作，会被OpenVINO编译器调度到DSP和DMA处理。
        *   **方案**：将动态形状降为静态1-D/2-D形状以消除Reshape节点；预计算所有需要转置的矩阵（如FT, HT, HTneg）并将其作为常量折叠进计算图，消除运行时Transpose节点。
        *   **结果**：计算图仅剩DPU原生操作（MatMul, Add, Concat）。
    3.  **块对角批处理并行化**：
        *   **问题**：单个滤波器张量尺寸小，无法充分利用NPU的大规模MAC阵列。
        *   **方案**：对于N个独立滤波器，将每个滤波器的系统矩阵（如状态转移矩阵F）扩展成一个维度为(N·n) × (N·n)的块对角矩阵（n为单滤波器状态维度，N=200）。
        *   **结果**：将N个不相关的滤波器打包成一个大型矩阵乘加调用，显著提升计算粒度以摊销任务分派开销，且因块对角结构保证了滤波器间无数值耦合。

### 3. 实验设计

*   **硬件平台**：在两代代表性AI-PC平台上进行测试：Intel Core Ultra Series 1 和 Series 2 参考系统。每款芯片上的CPU、GPU和NPU均在相同机箱和默认散热策略下进行对比。
*   **基准对比方法**：在同一硬件上，比较了同一KF模型在**CPU**、**GPU**和**NPU**三个计算目标上的性能表现。
*   **软件工具链**：PyTorch → ONNX → Intel OpenVINO 2024.5 编译器。所有平台统一使用FP16精度。
*   **测试场景与工作负载**：
    *   **单实例滤波器**：LKF（状态维度n=6，3-D位置和速度），EKF（n=8，恒定转弯率带加速度），每次推理处理1个滤波器。
    *   **批处理滤波器（MOT模拟）**：通过块对角化技术，每次推理并行处理N=200个滤波器。
    *   **端到端验证**：将NPU上的KF集成到一个实时视频跟踪管道中，由CPU上的Haar级联检测器提供测量值，KF在NPU上进行状态估计，并在实时视频流上渲染跟踪框。
*   **评估指标**：延迟(Latency)，吞吐量(FPS)，有效功耗(W)，动态能耗(mJ)。

### 4. 资源与算力

*   **硬件规格**：论文明确指出了使用的SoC平台型号（Intel Core Ultra Series 1, Series 2），但**未披露**其具体的CPU核心数、GPU EU数量或NPU的MAC单元数量等详细规格。
*   **训练算力**：**不适用**。该工作是算法部署与推理优化，不涉及模型训练，因此无GPU训练时长等信息。
*   **推理功耗与能源**：详细测量并对比了各计算目标的推理功耗和动态能耗，这是论文的核心议题之一。

### 5. 实验数量与充分性

*   **实验矩阵**：实验设计较为全面，覆盖了系统性的多维度对比：
    *   **2个硬件平台** (S1, S2) × **3个计算目标** (CPU, GPU, NPU) × **2种滤波器** (LKF, EKF) × **4种优化配置** (基线, 优化1, 优化2, 批处理) = 共计48种组合情况的核心指标对比。
    *   提供了**消融分析**：通过Perfetto追踪，可视化了从基线到最终批处理版本每一步优化（减法消除、形状融合）对DPU/MAC占用率的提升效果，直接验证了每个优化步骤的有效性。
    *   进行了**真实场景验证**：将优化后的滤波器集成到视频跟踪demo中，证实了其在真实系统（<1%帧预算）中的可用性。
*   **客观性与公平性**：实验设计客观公平。所有方法（CPU, GPU, NPU）在同一硬件平台上、使用相同的工具链（OpenVINO 2024.5）和精度（FP16）进行对比。测量了全SoC有效功耗而非单一IP模块功耗，更贴近实际部署情况。

### 6. 论文的主要结论与发现

*   **高吞吐与低功耗并存**：在批处理（MOT）场景下，NPU展现出巨大优势。以Series 2为例，EKF达223.35 FPS (13.43W)，LKF达408.73 FPS (14.05W)，相比CPU实现，**动态能耗最高降低了97.9%**。
*   **计算效率根源**：通过KATANA的三步优化，成功将KF的所有计算100%映射到DPU矩阵引擎，消除了昂贵的DPU↔DSP上下文切换，使得DPU的MatMul操作占推理时间的~95%（LKF）和~80%（EKF）。
*   **平台生态优势**：该映射方法利用了SoC中的现有NPU，无需设计专用硬件加速器，可让CPU和GPU专注于检测和上层分析等它们擅长的任务。

### 7. 优点

*   **方法论简洁且高效**：提出的三步优化（减转加、静形状、块对角）针对性强，直击NPU架构痛点，无需修改算法本身，工程可复用性高。
*   **工作负载匹配的洞察深刻**：揭示了卡尔曼滤波这种“小数据密集线性代数”工作负载天然不适合单个串行CPU（功耗）或单个微小的NPU调度（开销），而适合通过块对角化形成“大数据密集线性代数”以发挥NPU的矩阵加速优势。
*   **验证链条完整**：从微观的算子级追踪分析（Perfetto）到宏观的系统性能与功耗测量，再到真实应用（视频跟踪demo）的端到端验证，证据链完整扎实。

### 8. 不足与局限

*   **与GPU的对比维度不完整**：论文指出GPU在高FPS的同时功耗也高，不适于电池供电设备。但未探究GPU在限定低功耗模式（如cTDP）下的性能表现，未能给出等CPU/GPU功耗下的能效对比。
*   **通用性与可扩展性存疑**：
    *   **状态维度限制**：块对角批处理将一个(N·n)²大小的矩阵送入NPU，对于状态维度n非常大（如用于SLAM）的滤波器，该方法可能导致显存或内存带宽瓶颈。
    *   **非独立场景不适用**：块对角并行化方法明确要求各滤波器状态转移阵F和观测阵H相互独立。对于目标间存在交互或关联的复杂滤波模型（如JPDA），该方法存在局限。
*   **软件框架依赖性**：KATANA的优化脚本和最终性能与Intel OpenVINO编译器的行为紧密相关，若迁移至其他NPU硬件和编译工具链（如Apple Core ML, Qualcomm QNN），无法保证能直接复现完全纯DPU运算的效果。
*   **缺乏精度量化分析**：文中提到统一使用FP16，但未提供FP16 KF与标准FP32算法的数值误差对比分析，这对于高精度的状态估计应用是关键信息。

（完）

---
title: Regularised Iterative Generalised Least Squares with Optimal Selection of the Hyper-Parameter for Identifying Nonlinear Phenomenological Models
title_zh: 具有超参数最优选择的正则化迭代广义最小二乘法用于非线性唯象模型辨识
authors: "Mark Cary, Charles Bokor"
date: 2026-08-19
pdf: "https://arxiv.org/pdf/2608.18742v1"
tags: ["query:soc-soh"]
score: 8.0
evidence: 针对锂离子电池健康状态预测，采用正则化回归进行参数估计
tldr: 在锂离子电池健康状态预测等依赖经验模型的领域，准物理现象学模型常因参数混杂而难以可靠估计。本文提出一种正则化迭代广义最小二乘方法，引入岭回归改善数值条件，并基于信息论准则在每次迭代自动选择最优超参数，通过不动点迭代快速求解。仿真证实该方法能同时拟合异方差和序列相关数据，有效提升非线性现象学模型的参数辨识能力，为相关应用提供了稳健的估计框架。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.18742v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1342, \"height\": 669, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.18742v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1381, \"height\": 686, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.18742v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1487, \"height\": 816, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.18742v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1526, \"height\": 813, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.18742v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1509, \"height\": 811, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.18742v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 910, \"height\": 316, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.18742v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1489, \"height\": 432, \"label\": \"Table\"}]"
motivation: 锂离子电池等领域现象学模型参数常因结构混杂难以可靠估计，需要改进数值条件。
method: 提出正则化迭代广义最小二乘方法，引入岭回归，基于信息论准则每次迭代自动优化超参数，采用不动点迭代快速求解。
result: 仿真证实该方法能有效处理异方差和序列相关数据，改善非线性现象学模型参数辨识的数值条件。
conclusion: 为非线性现象学模型辨识提供了稳健正则化估计工具，可推广至电池健康预测等依赖经验模型的领域。
---

## 摘要
在一些目前以经验方法为主的领域，例如锂离子电池健康状态（SoH）预测中，受准物理思维启发的唯象模型包含需要从实验数据中估计的参数。此类模型的结构往往会产生完全或部分混杂的参数，使其难以甚至无法可靠估计。为了保留所需的模型形式并同时改善该问题的数值条件，我们引入岭回归方案。基于模型性能的信息论度量，提供了一种自动化方法，在每次迭代中优化岭回归超参数。所给出的公式需要不动点迭代来求解超参数。分析表明，在给定合适初始值的情况下，收敛非常迅速。最优超参数选择机制被整合到一个高效的正则化迭代广义最小二乘机制中，能够根据需要拟合异方差和序列相关数据。仿真验证了整体方法的有效性。

## Abstract
In some fields currently dominated by empirical approaches, such as state of health (SoH) prediction for lithium-ion batteries, phenomenological models motivated by quasi-physical thinking contain parameters to be estimated from experimental data. Often the structure of such models yields fully or partially confounded parameters, which are difficult or even impossible to estimate reliably. To preserve the desired model formulation and simultaneously improve the numerical conditioning for the problem we introduce a ridge regression scheme. An automated method is provided, based on information theoretic measures of model performance, which optimises the ridge regression hyper-parameter at each iteration. The formulae presented require fixed point iteration to solve for the hyper-parameter. Given a suitable starting value, analysis demonstrates convergence is very rapid. The optimal hyper-parameter selection mechanism is incorporated within an efficient regularised iterative generalised least squares mechanism, capable of fitting both heteroscedastic and serially correlated data as required. Simulation confirms the efficacy of the overall method.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究背景**：在锂离子电池健康状态（SoH）预测等领域，常用由准物理思想驱动的唯象模型，例如 Martinez-Laserna 容量衰减模型中包含指数项、幂律项和增益系数的乘积形式。
- **核心问题**：此类模型往往具有完全或部分混杂的参数，导致模型不可识别或数值条件极差。具体表现为 Jacobian 矩阵近共线，信息矩阵（\(J^T J\)）近似秩亏，参数估计对数据扰动非常敏感，普通最小二乘无法得到可靠结果。
- **整体含义**：为了在保留原物理模型结构的前提下改善数值条件，论文提出通过岭回归引入正则化，并在每次迭代中基于信息论准则自动选择最优超参数，形成正则化的迭代广义最小二乘方法，以提升参数辨识的稳定性与精度。

## 2. 论文提出的方法论

- **核心思想**：在负对数似然函数中加入岭回归惩罚项 \(\lambda \theta^T \theta\)，通过超参数 \(\lambda\) 在数据拟合与模型约束之间取得平衡，将病态问题转化为良态问题。
- **关键技术细节**：
  - 假设误差服从高斯分布，允许一般协方差结构（可包含异方差和序列相关），并通过 Cholesky 分解 \(W = C^T C\) 将加权回归转化为普通形式。
  - 对非线性模型采用一阶泰勒展开线性化，得到带正则项的线性化目标函数，其解为 \(\Delta\theta = (Z^T Z + \lambda I)^{-1} Z^T q\)。
  - 引入平滑矩阵 \(S(\lambda) = Z A^{-1} Z^T\)，有效自由度 \(\gamma = \mathrm{tr}(S)\)，并给出剖面负对数似然 \(L_p(\lambda)\) 及其对 \(\lambda\) 的导数。
  - 基于两个信息论准则 \(AIC_c\) 和 \(BIC\)，将各自对 \(\lambda\) 的导数置零，推导出不动点迭代公式：
    - \(\lambda_{BIC} = \frac{\sigma^2 \log(N) \mathrm{tr}(A^{-1} - \lambda A^{-2})}{2N q^T Z A^{-3} Z^T q}\)
    - \(\lambda_{AIC_c} = \frac{\sigma^2 \left[ \frac{(2\gamma+1)}{(N-\gamma-1)} - \frac{\gamma(\gamma+1)}{(N-\gamma-1)^2} \right] \mathrm{tr}(A^{-1} - \lambda A^{-2})}{N q^T Z A^{-3} Z^T q}\)
  - 由于 \(\lambda\) 同时出现在等式两边，采用固定点迭代求解，并通过收敛定理保证在合适的初始区间内快速收敛。
- **算法流程（RIGLS）**：
  1. 初始化 \(\theta\)，利用正则化非线性最小二乘（\(W=I\)）估计 \(\theta\)，并用固定点迭代优化 \(\lambda\)。
  2. 利用当前 \(\theta\) 计算残差，估计协方差模型参数 \(\delta\)。
  3. 用估计的协方差矩阵更新 \(\theta\)，同时再次迭代更新 \(\lambda\)。
  4. 判断参数收敛，输出最终 \(\theta\)、\(\delta\) 和 \(\sigma^2\)。
  - 实现中使用了 MATLAB 的 `fmincon`，并提供了解析梯度以降低计算成本。

## 3. 实验设计

- **数据集/场景**：使用模拟数据，基于 Martinez-Laserna 锂电池容量衰减模型 \(Q_{loss} = \omega \exp(\beta_1/T + \beta_2 \overline{SOC}) Ah^z\)。
- **真实参数**：\(\omega=0.4\)，\(\beta_1=0.09\)，\(\beta_2=0.02\)，\(z=0.5\)，并乘有系数 100。
- **噪声设置**：加入高斯异方差噪声 \(\mathcal{N}(0, \sigma^2 Q_{loss}^{2\delta})\)，其中 \(\sigma=0.05\)，\(\delta=1\)；生成 51 个等距点，\(Ah \in [0,50]\)，拟合前将 \(Ah\) 和 \(Q_{loss}\) 归一化到 \([0,1]\)。
- **对比方法**：
  - ROLS（正则化普通最小二乘）：仅执行 RIGLS 第 1 步，未考虑协方差结构。
  - RIGLS 基于 \(AIC_c\) 选择超参数。
  - RIGLS 基于 \(BIC\) 选择超参数。
- **评价指标**：诊断图（残差 vs 预测）、条件数 \(\mu_0\)（未正则化）和 \(\mu_1\)（正则化）、有效自由度 \(\gamma\)、参数估计及 95% 置信区间、超参数收敛行为分析。

## 4. 资源与算力

- 论文未提及 GPU 型号、数量、训练时长等算力信息。
- 实验使用 MATLAB 实现，调用 `fmincon` 优化工具箱，未报告计算资源消耗或运行时间。因此无法从文中获知具体算力需求。

## 5. 实验数量与充分性

- **实验数量**：
  - 核心实验为 1 个模拟数据场景，比较了 3 种估计方案（ROLS、RIGLS-AICc、RIGLS-BIC）。
  - 此外进行了超参数固定点迭代的收敛性分析（不同起始值、不同 \(k\) 下的迭代次数）。
- **充分性与客观性**：
  - 实验规模较小，仅基于单个模拟场景，参数维度较低（4 个参数），样本量 51，覆盖面有限。
  - 但与真实参数直接对比，能客观展示估计偏差、条件数改善和置信区间性质。
  - 对比了两种信息论准则，但未与其他正则化超参数选择方法（如 GCV、L-curve 等）或非正则化方法进行系统比较，公平性主要体现在相同模拟数据和相同算法框架内。

## 6. 论文的主要结论与发现

- 基于固定点迭代的超参数优化方法在合适初值下收敛迅速（约 7-8 次迭代达到误差界 0.00001）。
- RIGLS 能有效处理异方差数据，估计出的协方差参数 \(\delta=0.973\)，接近真值 1；诊断图显示加权残差异方差问题得到改善。
- 即使超参数值很小，条件数从约 \(10^{18}\) 大幅降至约 \(10^2\)–\(10^3\)，有效自由度从 4 降至约 2，说明正则化显著改善了数值条件。
- 在模拟研究中，基于 \(AIC_c\) 的 RIGLS 略优于基于 \(BIC\) 的 RIGLS。
- 参数估计整体接近真值，但正则化引入了偏差；部分参数（如 \(\beta_1\)）的置信区间不包含真值，且 BIC 情况下 \(\beta_1\) 估计符号错误，可能影响对老化机制的物理解释。
- 置信区间宽度与条件数相关，虽然超参数较大的 BIC 估计条件数更大，导致置信区间也更宽。

## 7. 优点

- **方法创新性**：将信息论准则（AICc、BIC）与固定点迭代结合，自动选择岭回归超参数，无需手动调节。
- **理论推导完整**：给出了平滑矩阵、有效自由度、剖面似然对 \(\lambda\) 的导数及固定点迭代公式的详细推导，并提供了收敛定理和误差界。
- **适用范围广**：允许异方差和序列相关误差，采用加权广义最小二乘框架，比普通最小二乘的正则化方法更具一般性。
- **数值稳定性**：通过正则化大幅降低信息矩阵条件数，提高参数估计精度和稳定性，同时尽可能保留原物理模型结构。
- **算法实现清晰**：给出了完整的 RIGLS 迭代步骤，并提供解析梯度以减少有限差分带来的计算负担。

## 8. 不足与局限

- **实验覆盖有限**：仅使用一个模拟数据场景，样本量小（51 点）、参数少（4 个），未在多个电池数据集或不同老化条件下验证。
- **偏差问题**：正则化固有偏差导致部分置信区间不包含真值，某些参数符号估计错误，可能误导物理机制解释。
- **对比方法不足**：仅比较了 AICc 和 BIC，未与 GCV、L-curve、经验贝叶斯等其他超参数选择方法对比；也未与非线性混合效应模型或贝叶斯方法比较。
- **初始值敏感性**：固定点迭代收敛依赖初始 \(\lambda\) 的选择，虽然给出了选择策略，但在某些区间可能出现不稳定或返回负值。
- **实际应用未验证**：论文仅使用模拟数据，未在真实锂离子电池老化数据上测试，实际性能尚待检验。
- **超参数与协方差参数联合估计**：文中虽扩展至异方差/序列相关，但未深入讨论不同协方差模型假设下的稳健性，也未考虑模型误设对超参数选择的影响。

（完）
